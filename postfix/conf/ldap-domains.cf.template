server_host = ldap://{{LDAP_HOSTNAME}}
start_tls = yes
bind = yes
bind_dn = {{LDAP_DN}}
bind_pw = {{LDAP_PASSWORD}}
version = 3
search_base = {{LDAP_DOMAINS}}
query_filter = (&(objectClass=dcObject)(dc=%s))
result_attribute = dc